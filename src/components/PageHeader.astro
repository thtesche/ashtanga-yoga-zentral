---
/**
 * Shared page header for all content pages (EN + DE).
 *
 * Replaces the duplicated `<div class="page-header">` markup that used to
 * live in 14 MDX files. All header styles (default + legal-page variants)
 * are scoped here, so the per-page CSS files no longer define them.
 *
 * Usage in MDX (no import needed — auto-discovered via mdxComponents):
 *   <PageHeader title="FAQ" subtitle="Frequently Asked Questions" />
 *   <PageHeader title="Privacy Policy" class="gdpr-header" />
 */
interface Props {
  /** Page heading (H1). */
  title: string;
  /** Optional line below the heading. Omit for header-only pages (e.g. GDPR). */
  subtitle?: string;
  /** Optional extra class for page-specific header variants (e.g. "gdpr-header"). */
  class?: string;
}

const { title, subtitle, class: variantClass } = Astro.props;
---

<div class:list={["page-header", variantClass]}>
  <div class="container text-center animate-fade-in">
    <h1>{title}</h1>
    {subtitle && <p class="subtitle">{subtitle}</p>}
  </div>
</div>

<style>
  /* Default variant: dark primary header. */
  .page-header {
    background-color: var(--color-primary);
    color: #fff;
    padding: 6rem 0 4rem;
  }

  .page-header h1 {
    color: #fff;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: #ffffffe6;
  }

  /* Light variants for the legal pages (GDPR / Impressum). Scoped like
     .page-header, so they must stay after it to win at equal specificity. */
  .gdpr-header {
    background-color: var(--color-info, #e9f1ff);
    color: #333;
    padding: 4rem 0 2rem;
  }

  .gdpr-header h1 {
    color: #0056b3;
    margin-bottom: 0.5rem;
  }

  .impressum-header {
    background-color: var(--color-info, #e9f1ff);
    color: #333;
    padding: 4rem 0 2rem;
  }

  .impressum-header h1 {
    color: #0056b3;
    margin-bottom: 0.5rem;
  }
</style>
