---
title: Kontakt
description: Nimm Kontakt mit Elinore Burke auf für weitere Informationen oder um dich für den Newsletter anzumelden.
---
import MainLayout from '../../layouts/MainLayout.astro';

<MainLayout title={frontmatter.title}>

<div class="page-header">
  <div class="container text-center animate-fade-in">
    <h1>Kontakt</h1>
    <p class="subtitle">Lass uns in Kontakt bleiben!</p>
  </div>
</div>

<section class="section">
  <div class="container">
    <div class="contact-wrapper">
      <div class="contact-info surface animate-fade-in-delay">
        <p>Sende eine Nachricht für weitere Informationen oder um dich für meinen Newsletter anzumelden!</p>
        <div class="info-block mt-4">
          <h3>Standort</h3>
          <p><strong>Three Boons Studio</strong><br/>Brunnenstr. 29/3 (Hinterhof)<br/>10119 Berlin</p>
        </div>
        <div class="info-block">
          <h3>Studio-Informationen</h3>
          <p>Wenn du neu im Yoga bist, plane bitte etwa 30 Minuten für deine erste Klasse ein. Anfänger sind immer willkommen.</p>
        </div>
      </div>
      <div class="contact-form surface animate-fade-in-delay">
        <form id="form">
          <input type="hidden" name="access_key" value={import.meta.env.PUBLIC_WEB3FORMS_ACCESS_KEY} />
          <div class="form-group">
            <label for="name">Name (erforderlich)</label>
            <input type="text" id="name" name="name" required />
          </div>
          <div class="form-group">
            <label for="email">E-Mail (erforderlich)</label>
            <input type="email" id="email" name="email" required />
          </div>
          <div class="form-group">
            <label for="message">Nachricht (erforderlich)</label>
            <textarea id="message" name="message" rows="6" required></textarea>
          </div>
          <button type="submit" class="btn btn-submit">Nachricht senden</button>
          <p class="faq-link">Vielleicht findest du Antworten auf unserer <a href="./faq">FAQ-Seite</a>.</p>
          <div id="result"></div>
        </form>
      </div>
    </div>
  </div>
</section>

</MainLayout>

<script>{`
  const form = document.getElementById("form");
  const result = document.getElementById("result");

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    result.innerHTML = "Sende...";

    const response = await fetch("https://api.web3forms.com/submit", {
      method: "POST",
      body: new FormData(form),
    });

    const data = await response.json();
    if (data.success) {
      result.innerHTML = "Vielen Dank für Ihre Nachricht. Sie wurde gesendet.";
      form.reset();
    } else {
      result.innerHTML = data.message;
    }
  });
`}</script>

<style>{`
.page-header{background-color:var(--color-primary);color:#fff;padding:6rem 0 4rem}
.page-header h1{color:#fff;margin-bottom:.5rem}
.subtitle{font-family:var(--font-heading);font-size:1.25rem;color:#ffffffe6}
.text-center{text-align:center}
.text-primary{color:var(--color-primary)}
.mt-4{margin-top:2rem}
.contact-wrapper{display:grid;grid-template-columns:1fr 1.5fr;gap:3rem;max-width:1000px;margin:-4rem auto 0;position:relative;z-index:10}
.info-block{margin-top:2rem;padding-top:2rem;border-top:1px solid var(--color-border)}
.info-block h3{font-size:1.25rem;color:var(--color-secondary);margin-bottom:.5rem}
.form-group{margin-bottom:.5rem}
.btn-submit{width:100%;margin-top:1rem;font-size:1.1rem}
#result{margin-top:1rem;padding:1rem;text-align:center;font-size:1rem}
.faq-link{margin-top:.75rem;text-align:center;font-size:.9rem;color:var(--color-text-secondary, #666)}.faq-link a{color:var(--color-primary);text-decoration:underline}
@media (max-width: 768px){.contact-info{display:none}.contact-wrapper{grid-template-columns:1fr;margin-top:0}}
`}</style>
